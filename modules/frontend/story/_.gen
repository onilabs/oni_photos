/**
   @summary Wildcard generator file for serving public photo stories
*/

@ = require([
  'mho:std',
  'mho:surface/html',
  {id: 'backend:db/stories', name: 'stories'}
]);

exports.filetype = 'html';

var CSS = `
  <style type="text/css">
  * {
    box-sizing: border-box;
  }
  body {
    margin: 0px;
    background-color: #fff;
    color: #6b6b6b;
    font-family: sans-serif;
  }
  .story-wrapper {
    width: 80%;
    max-width: 930px;
    margin: 100px auto;
  }
  .story-header {
    font-size: 36px;
    color: #333;
    margin: 0 0 50px 0;
  }
  .story-block {
    width: 50%;
    border-color: transparent;
    border-style: solid;
    border-width: 3px 0;
    display: block;
    float: left;
  }
  .story-block:nth-child(even) {
    border-left-width: 3px;
  }
  .story-block:nth-child(odd) {
    border-right-width: 3px;
  }
  .story-image {
    position:relative;
    width: 100%;
    height: 0;
    padding-bottom: 75%;
    background-repeat: no-repeat;
    background-position: center center;
    background-size: cover;
  }
  .story-image-zoomed {
    -webkit-transform: translateZ(0);
    -webkit-transition: all 250ms cubic-bezier(0.77, 0, 0.175, 1) 100ms;
    position: fixed;
    width: 400px;
    height: 300px;
    top: 50px;
    left: 50px;
    z-index: 5;
    background-repeat: no-repeat;
    background-position: center center;
    background-size: cover;
  }
  .story-fog {
    -webkit-transform: translateZ(0);
    transform: translateZ(0);
    -webkit-transition: all 200ms linear;
    transform: translateZ(0);
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 4;
    background: #111;
    opacity: .9;
    pointer-events: none;
  }
  .story-fog.hidden {
    opacity: 0;
  }
  @media (max-width: 530px) {
    .story-wrapper {
      width: 100%;
    }
  }
  </style>`;

exports.content = function(params) {

  try {
    var story_id = params.path.substring(1);

    function Block(descriptor) {
      var rv = @Div() .. @Class('story-block');
      if (descriptor.type === 'img')
        rv = rv .. @Content(@Div() .. @Class('story-image') .. @Attrib('style', 'background-image: url(' + descriptor.url + ')'));
      return rv;
    }
    return @Document({
      title: 'Photo Story',
      template: 'plain',
      externalScripts: ["/__mho/surface/bootstrap/jquery-1.10.2.min.js"],
      main: "./story.sjs",
      head: CSS
    }) :: @Div() .. @Class('story-wrapper') ::
      `
      <div class="story-header">Untitled story</div>
      <div>
      ${
        @stories.getPublicStory(story_id).content .. @map(
          row -> row .. @map(Block)
        )
      }
      </div>
      `;
  }
  catch (e) {
    console.log(e);
    return @Document({
      title: 'Error',
      template: 'plain'
    }) ::
      `Nothing to see here; move along`
  }
};
